import Image from "next/image";
import {createClient} from "@supabase/supabase-js";
const labels={top:"上装",bottom:"下装",shoes:"鞋履",bag:"包袋"} as const;
type WardrobeItem={id:string;name:string;category:keyof typeof labels;primary_color:string};
export default async function Closet({params}:{params:Promise<{code:string}>}){const {code}=await params;const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,{auth:{persistSession:false}});const {data}=await s.rpc("agent_list_wardrobe_items",{p_access_code:code,p_category:null,p_limit:50});const items:WardrobeItem[]=data?.items??[];return <main className="visual-closet"><header><span>AHA WARDROBE</span><h1>我的图片衣橱</h1><p>{items.length} 件单品 · 图片仅通过专属链接访问</p></header><div className="visual-grid">{items.map((x)=><article key={x.id}><div className="visual-photo"><Image unoptimized width={640} height={800} src={`/api/agent-media/${x.id}?code=${encodeURIComponent(code)}`} alt={x.name}/></div><h2>{x.name}</h2><p>{labels[x.category]} · {x.primary_color}</p></article>)}</div></main>}
