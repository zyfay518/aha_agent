import Image from "next/image";
import {notFound} from "next/navigation";
import {createClient} from "@supabase/supabase-js";
const labels={top:"上装",bottom:"下装",shoes:"鞋履",bag:"包袋"} as const;
const colorLabels:Record<string,string>={black:"黑色",white:"白色",navy:"藏青色",red:"红色",blue:"蓝色",gray:"灰色",grey:"灰色",beige:"米色",brown:"棕色",green:"绿色",yellow:"黄色",pink:"粉色",purple:"紫色",orange:"橙色"};
type WardrobeItem={id:string;name:string;category:keyof typeof labels;primary_color:string};
export default async function Closet({params}:{params:Promise<{code:string}>}){const {code:viewId}=await params;if(!/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(viewId))notFound();const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,{auth:{persistSession:false}});const {data,error}=await s.rpc("view_list_wardrobe_items",{p_view_id:viewId,p_limit:50});if(error)notFound();const items:WardrobeItem[]=data?.items??[];return <main className="visual-closet"><header><span>AHA WARDROBE</span><h1>我的图片衣橱</h1><p>{items.length} 件单品 · 只读专属链接</p></header><div className="visual-grid">{items.map((x)=><article key={x.id}><div className="visual-photo"><Image unoptimized width={640} height={800} src={`/api/agent-media/${x.id}?view=${encodeURIComponent(viewId)}`} alt={x.name}/></div><h2>{x.name}</h2><p>{labels[x.category]} · {colorLabels[x.primary_color]??x.primary_color}</p></article>)}</div></main>}
